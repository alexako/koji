services:
  brain:
    build: .
    container_name: koji-brain
    restart: unless-stopped
    ports:
      - "8585:8080"
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8080/health"]
      interval: 30s
      timeout: 5s
      retries: 3
